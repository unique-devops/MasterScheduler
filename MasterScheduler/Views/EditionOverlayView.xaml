<UserControl x:Class="MasterScheduler.Views.EditionOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MasterScheduler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#CC000000">
        <Border  Background="#1E1E1E" VerticalAlignment="Center" HorizontalAlignment="Center" 
                CornerRadius="10" Width="700" Padding="15" BorderBrush="#444" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Choose Your Edition" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Unlock the full power of Master Scheduler" Foreground="#AAA" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="5">
                        <TextBlock Text="LITE" Foreground="#AAA" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Free Forever" Foreground="White" FontSize="18" HorizontalAlignment="Center" Margin="0,5"/>
                        <Separator Background="#333" Margin="0,10"/>
                        <TextBlock Text="✓ Local Backups" Foreground="#888" Margin="0,5"/>
                        <TextBlock Text="✓ Manual Tasks" Foreground="#888" Margin="0,5"/>
                        <TextBlock Text="✗ No Scheduling" Foreground="#555" Margin="0,5"/>
                        <Button Content="{Binding LiteVersion}" IsEnabled="False" Margin="0,20,0,0" />
                    </StackPanel>

                    <Border Grid.Column="1" Background="#2A2A2A" CornerRadius="8" Padding="15" BorderBrush="#F59E0B" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="TRIAL" Foreground="#F59E0B" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="30 Days Full" Foreground="White" FontSize="18" HorizontalAlignment="Center" Margin="0,5"/>
                            <Separator Background="#444" Margin="0,10"/>
                            <TextBlock Text="✓ All Pro Features" Foreground="#CCC" Margin="0,5"/>
                            <TextBlock Text="✓ Auto Scheduling" Foreground="#CCC" Margin="0,5"/>
                            <TextBlock Text="✓ Cloud Sync" Foreground="#CCC" Margin="0,5"/>
                            <Button Content="{Binding TrialVersion}" IsEnabled="{Binding IsActiveTrial}" Command="{Binding ActivateTrialCommand}" Background="#F59E0B" Foreground="Black" FontWeight="Bold" Margin="0,20,0,0"/>
                        </StackPanel>
                        
                        
                    </Border>

                    <StackPanel Grid.Column="2" Margin="10">
                        <TextBlock Text="PRO" Foreground="#3B82F6" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="$XX.XX Once" Foreground="White" FontSize="18" HorizontalAlignment="Center" Margin="0,5"/>
                        <Separator Background="#333" Margin="0,10"/>
                        <TextBlock Text="✓ Lifetime Access" Foreground="#CCC" Margin="0,5"/>
                        <TextBlock Text="✓ Priority Support" Foreground="#CCC" Margin="0,5"/>
                        <TextBlock Text="✓ Commercial Use" Foreground="#CCC" Margin="0,5"/>
                        <Button Content="Buy Now" Background="#3B82F6" Foreground="White" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>

                <Button Content="Close" Command="{Binding GoBackCommand}" Background="Transparent" Foreground="#666" BorderThickness="0" Margin="0,30,0,0"/>
            </StackPanel>
        </Border>

        <Border Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="Activate Your Trial" Foreground="White" FontSize="20" HorizontalAlignment="Center"/>
                <TextBlock Text="Enter email to receive your trial key" Foreground="#AAA" Margin="0,10"/>

                <TextBox Text="{Binding UserEmail, UpdateSourceTrigger=PropertyChanged}" 
         Background="#333" Foreground="White" Padding="10" Margin="0,10"/>

                <Button Content="Confirm &amp; Activate" 
        Command="{Binding ActivateTrialCommand}"
        Background="#22C55E" Foreground="White" FontWeight="Bold"/>

                <Button Content="Go Back" 
        Command="{Binding ShowEmailFormCommand}" 
        CommandParameter="false" Background="Transparent" Foreground="#666"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
