<Window x:Class="MasterScheduler.Views.GoogleDriveConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MasterScheduler.Views"
        mc:Ignorable="d"
        Title="Google Drive Authorization" Height="380" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Google Drive Configuration" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Account Information" Margin="0,0,0,12">
                    <Grid Margin="12">
                        <Button x:Name="BtnAuthorize" Grid.Column="1" Content="Link Account" Click="BtnAuthorize_Click" Margin="10,0,0,0" Padding="10,2">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Content" Value="Authorize Google Drive" />
                                    <Setter Property="Background" Value="#4285F4" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Content" Value="Connected ✅" />
                                            <Setter Property="Background" Value="#34A853" />
                                            <Setter Property="IsEnabled" Value="False" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </GroupBox>

        <GroupBox Grid.Row="2" Header="Drive Folder" Padding="0,7" FontSize="16">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="TxtFolderPath" IsReadOnly="True" Text="MSDataBackups" Width="250"/>
                <Button x:Name="btnCreateDriveFoder" Margin="7,0" Content="Create" Width="70" Click="BtnCreateDriveFolder_Click"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Common settings" FontSize="16">
            <StackPanel Margin="5" Orientation="Horizontal">
                <TextBlock  Text="Delete backup older than" FontSize="16"/>
                <TextBox Margin="5,0"  Width="70" FontSize="14" MaxLength="3" HorizontalContentAlignment="Center"  Text="{Binding RetentionDays}">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="7"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="Days." FontSize="16"/>
            </StackPanel>
        </GroupBox>

        <Separator Grid.Row="4" Background="Red" Margin="0,20"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnTest" Content="Test" Margin="5" Background="LightGray" Click="btnTest_Click"/>
            <Button Content="Save Settings" Click="btnSave_Click" Margin="5" Background="SkyBlue"/>
        </StackPanel>
    </Grid>
</Window>
