<UserControl x:Class="MasterScheduler.Views.LogDashboardViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MasterScheduler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded" >
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Width="200" x:Name="SearchBox" TextChanged="FilterLogs"/>

            <TextBlock Text="Level:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <ComboBox Width="100" x:Name="LevelFilter" SelectionChanged="FilterLogs">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Information"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Error"/>
            </ComboBox>

            <Button Content="Clear All Logs" Click="ClearLogs" Margin="50,0,0,0" 
                    Background="#FF7272" Foreground="White" Padding="10,2"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="LogGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" AlternatingRowBackground="#F9F9F9" GridLinesVisibility="None">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='HH:mm:ss'}" Width="100"/>

                <DataGridTemplateColumn Header="Level" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="5,2" 
                                    Background="{Binding Level, Converter={StaticResource LevelToColorConverter}}">
                                <TextBlock Text="{Binding Level}" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Job ID" Binding="{Binding JobId}" Width="60"/>
                <DataGridTextColumn Header="Job Type" Binding="{Binding Type}" Width="60"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
