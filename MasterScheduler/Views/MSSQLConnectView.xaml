<Window x:Class="MasterScheduler.Views.MSSQLConnectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:MasterScheduler.Views"
        mc:Ignorable="d"
        Title="Connect to SQL Server"
        Height="360" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FAFAFA"
        FontFamily="Segoe UI">

    <Border CornerRadius="8" BorderBrush="#DDD" BorderThickness="1" Padding="10" Background="White">
        
        <Grid>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Orientation="Horizontal" Margin="10">
                    <fa:ImageAwesome Icon="Server" Width="32" Height="32" Foreground="#4A90E2"/>
                    <TextBlock Margin="10,0" Foreground="#333" FontSize="20" FontWeight="SemiBold"
                   VerticalAlignment="Center" Text="Microsoft SQL Server"/>
                </StackPanel>

                <!-- Form -->
                <Grid Grid.Row="1" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Server name"/>
                    <ComboBox Grid.Row="0" Grid.Column="2" Margin="0,5" Height="28"
                  ItemsSource="{Binding Servers}" SelectedItem="{Binding SelectedServer}"
                  IsEditable="True" IsTextSearchEnabled="True"
                  Padding="5" BorderBrush="#CCC" BorderThickness="1" />

                    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Authentication"/>
                    <ComboBox Grid.Row="1" Grid.Column="2" Margin="0,5" Height="28"
                  ItemsSource="{Binding Authentications}" SelectedItem="{Binding SelectedAuthentication}"
                  Padding="5" BorderBrush="#CCC" BorderThickness="1"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Login ID"/>
                    <TextBox Grid.Row="2" Grid.Column="2" Margin="0,5" Height="28"
                 Text="{Binding LoginID}" Padding="5" BorderBrush="#CCC" BorderThickness="1"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="Password"/>
                    <TextBox Grid.Row="3" Grid.Column="2" Margin="0,5" Height="28"
                     Text="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Padding="5" BorderBrush="#CCC" BorderThickness="1"/>

                    <CheckBox Grid.Row="4" Grid.Column="2" Content="Remember password"
                  IsChecked="{Binding IsRemember, Mode=TwoWay}" Margin="0,5" Foreground="#555"/>
                </Grid>

                <!-- Separator -->
                <Separator Grid.Row="2" Margin="10,10,10,5" Background="#DDD"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                    <Button Content="Connect" Width="100" Height="32" Margin="5,0"
                Command="{Binding ConnectCommand}" 
                Background="#4A90E2" Foreground="White" BorderThickness="0"
                Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#357ABD"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Cancel" Width="100" Height="32" Margin="5,0"
                Command="{Binding CancelCommand}" 
                Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#CCCCCC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

            </Grid>
            <!-- Spinner Overlay -->
            <Grid Visibility="{Binding IsConnecting, Converter={StaticResource BoolToVisibilityConverter}}"
      Background="#80000000"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      Panel.ZIndex="99"
      IsHitTestVisible="True">

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Spinner -->
                    <Canvas Width="50" Height="50">
                        <Path Name="spinnerArc"
                  Stroke="DodgerBlue"
                  StrokeThickness="4"
                  StrokeStartLineCap="Round"
                  StrokeEndLineCap="Round"
                  Data="M 25,5 A 20,20 0 1 1 24.99,5"
                  RenderTransformOrigin="0.5,0.5">
                            <Path.RenderTransform>
                                <RotateTransform x:Name="spinnerRotate" Angle="0"/>
                            </Path.RenderTransform>
                            <Path.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                 From="0" To="360" Duration="0:0:1" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Path.Triggers>
                        </Path>
                    </Canvas>

                    <TextBlock Text="Connecting..."
                   Margin="0,10,0,0"
                   Foreground="White"
                   FontSize="14"
                   HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
