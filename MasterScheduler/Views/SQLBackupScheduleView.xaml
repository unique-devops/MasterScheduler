<UserControl x:Class="MasterScheduler.Views.SQLBackupScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:MasterScheduler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Black">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Foreground="White" Background="Transparent" Command="{Binding GoBackCommand}" Width="50">
                <fa:ImageAwesome Icon="ArrowLeft" Foreground="White" VerticalAlignment="Center" Width="16" Height="16" Margin="0,0,5,0"/>
            </Button>
            <!-- Centered Title -->
            <TextBlock Grid.Column="1" Text="SQL Backup Schedule" Foreground="White"
                FontSize="20" FontWeight="Bold" 
                VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <!-- Empty space (right) to balance layout -->
            <Border Grid.Column="2" Width="40"/>
        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="45"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <fa:ImageAwesome Grid.Column="0" Icon="KeyboardOutline" Width="25" Height="25" Foreground="red"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock FontWeight="Bold" Text="Connect To SQL Server"/>
                        <TextBlock  Text="Server Name.."/>
                    </StackPanel>
                    <fa:ImageAwesome Grid.Column="2" Icon="Edit" Width="25" Height="25" Foreground="red"/>


                    <fa:ImageAwesome Grid.Column="0" Grid.Row="1" Icon="KeyboardOutline" Width="25" Height="25" Foreground="red"/>
                    <StackPanel Grid.Column="1" Grid.Row="1">
                        <TextBlock FontWeight="Bold" Text="Select Database"/>
                        <ListView ItemsSource="{Binding SelectedDatabases}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                    <Button Grid.Column="2" Grid.Row="1" Command="{Binding OpenDatabaseSelectionCommand}" Background="Transparent">
                        <fa:ImageAwesome  Icon="Edit" Width="25" Height="25" Foreground="red"/>
                    </Button>
                    
                </Grid>
                
            </ScrollViewer>
        </Grid>

        <!--footer-->
        <Grid Grid.Row="2" Background="Black">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Width="40"/>
            <!-- Empty space (right) to balance layout -->
            <Border Grid.Column="1" Width="40"/>

            <Button Grid.Column="2" Background="Black" Margin="5"
                 Foreground="White" FontWeight="Bold" Cursor="Hand" Command="{Binding NextCommand}" Width="100">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Save" Margin="0,0,7,0" />
                    <fa:ImageAwesome Icon="Save" Foreground="White" VerticalAlignment="Center" Width="16" Height="16" Margin="0,0,5,0"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
