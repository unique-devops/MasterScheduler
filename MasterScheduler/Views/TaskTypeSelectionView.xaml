<UserControl x:Class="MasterScheduler.Views.TaskTypeSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:MasterScheduler.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" CornerRadius="12" Background="Transparent" Width="30" Height="30">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="White">
            <Button HorizontalAlignment="Left" Style="{StaticResource IconButton}" Margin="10,0" Command="{Binding GoBackCommand}">
                <fa:ImageAwesome Icon="ArrowLeft" Foreground="#333" Width="14"/>
            </Button>
            <TextBlock Text="Select Job Task" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Border BorderThickness="0,0,0,1" BorderBrush="#DDD" VerticalAlignment="Bottom"/>
        </Grid>
       
        <StackPanel Grid.Row="1" Margin="5">
            <Grid  Width="320">
                <Border CornerRadius="5"
                BorderBrush="#CCC"
                BorderThickness="1"
                Background="White"
                Padding="4">
                    <DockPanel>
                        <TextBlock Text="🔍"
                   FontSize="16"
                   VerticalAlignment="Center"
                   Margin="0,0,5,0"
                   DockPanel.Dock="Left"/>
                        <TextBox x:Name="SearchBox"
                     BorderThickness="0"
                     Background="Transparent"
                     VerticalAlignment="Center"
                     FontSize="14"
                     Padding="2"
                     TextChanged="SearchBox_TextChanged"/>
                    </DockPanel>
                </Border>

                <!-- Placeholder Text -->
                <TextBlock Text="Search..."
               Foreground="Gray"
               Margin="35   ,0,0,0"
               VerticalAlignment="Center"
               IsHitTestVisible="False"
               FontSize="14"
               x:Name="PlaceholderText"
               Visibility="Visible"/>
            </Grid>
        </StackPanel>

        <ListBox x:Name="JobTypeList" ItemsSource="{Binding TaskTypeList}" Grid.Row="2" 
                 SelectedItem="{Binding SelectedTaskType}" SelectionMode="Single" BorderThickness="0" Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="CardBorder" Margin="10" Width="180" Height="200" 
                                        Background="White" CornerRadius="15" BorderThickness="2" BorderBrush="Transparent">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.1"/>
                                    </Border.Effect>

                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Grid Width="80" Height="80">
                                            <Ellipse Fill="#F1F2F6"/>

                                            <Image Width="45" Height="45" 
                                           Source="{Binding JobType, Converter={StaticResource JobTypeIconConverter}}"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Grid>
                                        <!--<Ellipse Width="60" Height="60" Fill="{TemplateBinding Foreground}" Opacity="0.1"/>-->
                                        <TextBlock Text="{Binding Name}" Margin="0,15,0,5" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Description}" FontSize="12" Foreground="Gray" TextWrapping="Wrap" TextAlignment="Center" Margin="10,0"/>
                                    </StackPanel>
                                </Border>

                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CardBorder" Property="BorderBrush" Value="#0984E3" />
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="CardBorder" Property="BorderBrush" Value="#0984E3" />
                                        <Setter TargetName="CardBorder" Property="Background" Value="#E1F5FE" />
                                        <Setter Property="Foreground" Value="#0984E3"/>
                                    </MultiTrigger>
                                    <Trigger Property="IsFocused" Value="True">
                                        <Setter TargetName="CardBorder" Property="BorderThickness" Value="3" />
                                        <Setter TargetName="CardBorder" Property="BorderBrush" Value="#74B9FF" />
                                    </Trigger>
                                    
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <!--footer-->
        <Grid Grid.Row="3" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Width="40"/>
            <!-- Empty space (right) to balance layout -->
            <Border Grid.Column="1" Width="40"/>

            <Button Grid.Column="2" Background="DeepSkyBlue" Margin="5"
     Foreground="White" FontWeight="Bold" Cursor="Hand" Command="{Binding NextCommand}" Width="100">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Next" Margin="0,0,7,0" />
                    <fa:ImageAwesome Icon="ArrowCircleOutlineRight" Foreground="White" VerticalAlignment="Center" Width="16" Height="16" Margin="0,0,5,0"/>
                </StackPanel>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Grid.Column="3" Background="Red" Margin="5"
            Foreground="White" FontWeight="Bold" Cursor="Hand" Command="{Binding GoBackCommand}" Width="100">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Cancel" Margin="0,0,7,0" />
                    <fa:ImageAwesome Icon="Remove" Foreground="White" VerticalAlignment="Center" Width="12" Height="12" Margin="0,0,5,0"/>
                </StackPanel>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</UserControl>
